apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rio-rrb-api.fullname" . }}-test-connection"
  labels:
    {{- include "rio-rrb-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rio-rrb-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
