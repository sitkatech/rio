apiVersion: v1
kind: Service
metadata:
  name: {{ include "rio-edf-geoserver.fullname" . }}
  labels:
    {{- include "rio-edf-geoserver.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rio-edf-geoserver.selectorLabels" . | nindent 4 }}
