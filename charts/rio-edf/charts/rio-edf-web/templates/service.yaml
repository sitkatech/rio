apiVersion: v1
kind: Service
metadata:
  name: {{ include "rio-edf-web.fullname" . }}
  labels:
    {{- include "rio-edf-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      #nodePort: 30081
      protocol: TCP
      name: http
  selector:
    {{- include "rio-edf-web.selectorLabels" . | nindent 4 }}
