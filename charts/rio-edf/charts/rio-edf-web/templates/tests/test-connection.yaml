apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rio-edf-web.fullname" . }}-test-connection"
  labels:
    {{- include "rio-edf-web.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rio-edf-web.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
