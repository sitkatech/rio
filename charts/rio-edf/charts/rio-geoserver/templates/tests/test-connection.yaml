apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rio-geoserver.fullname" . }}-test-connection"
  labels:
    {{- include "rio-geoserver.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rio-geoserver.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
