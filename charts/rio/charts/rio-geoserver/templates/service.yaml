apiVersion: v1
kind: Service
metadata:
  name: {{ include "rio-geoserver.fullname" . }}
  labels:
    {{- include "rio-geoserver.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rio-geoserver.selectorLabels" . | nindent 4 }}
