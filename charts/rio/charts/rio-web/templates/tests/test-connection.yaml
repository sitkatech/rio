apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rio-web.fullname" . }}-test-connection"
  labels:
    {{- include "rio-web.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rio-web.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
