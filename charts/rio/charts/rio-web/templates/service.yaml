apiVersion: v1
kind: Service
metadata:
  name: {{ include "rio-web.fullname" . }}
  labels:
    {{- include "rio-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      #nodePort: 30081
      protocol: TCP
      name: http
  selector:
    {{- include "rio-web.selectorLabels" . | nindent 4 }}
