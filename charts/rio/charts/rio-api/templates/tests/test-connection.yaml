apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rio-api.fullname" . }}-test-connection"
  labels:
    {{- include "rio-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rio-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
